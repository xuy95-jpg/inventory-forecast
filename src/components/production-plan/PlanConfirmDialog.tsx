'use client';

/**
 * Production plan confirmation dialog (reserved for future use)
 * Currently handled inline in PlanTable
 */

export default function PlanConfirmDialog() {
  // Placeholder for future modal confirmation flow
  return null;
}
